<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Login - WhatsApp Gateway</title>
  <!-- Bootstrap CSS (menggunakan CDN Bootstrap 5) -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome untuk ikon (opsional) -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    body {
      /* Gradient hijau ala WhatsApp */
      background: linear-gradient(135deg, #25D366 0%, #128C7E 100%);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .login-container {
      max-width: 420px; /* Sedikit lebih lebar */
      width: 100%;
      padding: 2.5rem; /* Padding lebih besar */
      background: #fff;
      border-radius: 12px; /* Border radius lebih besar */
      box-shadow: 0 8px 25px rgba(0,0,0,0.15); /* Bayangan lebih menonjol */
      text-align: center; /* Pusatkan konten di dalam container */
    }
    .login-header {
      margin-bottom: 1.5rem;
    }
    .login-header i {
      font-size: 3rem; /* Ukuran ikon */
      color: #075E54; /* Warna ikon hijau tua WhatsApp */
      margin-bottom: 0.5rem;
    }
    .login-header h2 {
      color: #128C7E; /* Warna judul hijau WhatsApp */
      font-weight: 600;
    }
    .form-control {
      border-radius: 6px; /* Border radius untuk input */
      padding: 0.75rem 1rem; /* Padding input */
    }
    .form-control:focus {
      border-color: #25D366;
      box-shadow: 0 0 0 0.25rem rgba(37, 211, 102, 0.25);
    }
    .btn-primary {
      background-color: #128C7E; /* Warna tombol utama hijau tua */
      border-color: #128C7E;
      padding: 0.75rem;
      font-size: 1rem;
      font-weight: 500;
      border-radius: 6px;
      transition: background-color 0.2s ease-in-out;
    }
    .btn-primary:hover {
      background-color: #075E54; /* Warna hover lebih gelap */
      border-color: #075E54;
    }
    .alert-danger {
      background-color: #f8d7da;
      border-color: #f5c6cb;
      color: #721c24;
      border-radius: 6px;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="login-header">
      <!-- Ganti dengan ikon yang lebih relevan jika ada, misal SVG atau gambar -->
      <i class="fas fa-paper-plane"></i> <!-- Contoh ikon Font Awesome -->
      <h2>WhatsApp Gateway</h2>
    </div>

    <% if (typeof message !== 'undefined' && message && message.length) { %>
      <div class="alert alert-danger" role="alert">
        <%= message %>
      </div>
    <% } %>

    <form action="/login" method="post">
      <div class="mb-3 text-start">
        <label for="username" class="form-label">Username</label>
        <input type="text" class="form-control" id="username" name="username" placeholder="Masukkan username" required>
      </div>
      <div class="mb-3 text-start">
        <label for="password" class="form-label">Password</label>
        <input type="password" class="form-control" id="password" name="password" placeholder="Masukkan password" required>
      </div>
      <div class="d-grid mt-4">
        <button type="submit" class="btn btn-primary">Masuk</button>
      </div>
    </form>
  </div>
  <!-- Bootstrap JS Bundle (dengan Popper) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
